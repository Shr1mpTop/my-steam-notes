[project]
name = "my-steam-notes"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "libsql-client>=0.3.1",
    "python-dotenv>=1.2.2",
    "requests>=2.33.1",
]
